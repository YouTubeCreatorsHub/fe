# dependencies(의존성)
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing(테스트)
/coverage

# production build(프로덕션 빌드)
/build
/dist
/out
/.next

# environment variables(환경 변수)
.env*
.env.local
.env.development.local
.env.test.local
.env.production.local

# logs(로그 파일)
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# system files(시스템 파일)
.DS_Store
*.pem
Thumbs.db

# IDE/editors(개발 도구/에디터)
.idea/
.vscode/
*.swp
*.swo

# TypeScript(타입스크립트)
*.tsbuildinfo
next-env.d.ts

# Deployment(배포)
.vercel

# cache(캐시)
.cache/
.eslintcache
